apiVersion: v1
kind: Service
metadata:
  name: lionsforge-backend
  labels:
    app: lionsforge-backend
spec:
  type: ClusterIP
  selector:
    app: lionsforge-backend
  ports:
    - name: http
      port: 80
      targetPort: 8000
