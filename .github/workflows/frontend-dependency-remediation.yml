name: Frontend Dependency Remediation

on:
  push:
    branches:
      - fix/frontend-dependency-security
  workflow_dispatch:

permissions:
  contents: read

jobs:
  regenerate-lockfile:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Regenerate patched lockfile
        shell: bash
        run: |
          set -euo pipefail
          npm install --package-lock-only --save-exact next@15.5.21
          npm update --package-lock-only sharp@0.35.0 postcss@8.5.10
          npm ci

      - name: Audit production dependencies
        run: npm audit --omit=dev --audit-level=moderate

      - name: Run frontend tests
        run: npm test

      - name: Build frontend
        run: npm run build

      - name: Record resolved security package versions
        shell: bash
        run: |
          npm ls next sharp postcss --all > dependency-versions.txt
          npm audit --omit=dev --json > npm-audit-production.json

      - name: Upload regenerated dependency files
        uses: actions/upload-artifact@v4
        with:
          name: frontend-patched-dependency-files
          path: |
            frontend/package.json
            frontend/package-lock.json
            frontend/dependency-versions.txt
            frontend/npm-audit-production.json
          if-no-files-found: error
          retention-days: 30
